Variables, Data Types, Operators

Concepts :

● let vs const :
let value reassign kore but const kore na means let can change the value but const cannot change the value. Same scope a both cannot be redeclared. Both are block scoped. Const must be initialize immediately.


● primitive vs reference types
● arithmetic & comparison operators
